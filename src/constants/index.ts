import type { TProduction } from "../types";

export const initValue: TProduction = {
    date:"",
    qtyMade: null,
    qtySold: null,
    dailyFee: null,
    ingredientCost: null,
    unitPrice: null
}